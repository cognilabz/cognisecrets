# v0.2.5 Release Notes

## Status

`v0.2.5` is a manifest-backed CogniSecrets release reconstructed from git history.

Release date: 2026-08-05

Tag commit:

```text
6463f16
```

Release image:

```text
ghcr.io/cognilabz/cognisecrets:v0.2.5
```

## Changes Since v0.2.4

- Reformatted the release Kustomization file.
- Updated the release manifest image tag to `v0.2.5`.

## API And Compatibility

The served API remains:

```text
cognilabz.com/v1
```

No user-facing API or controller behavior change is identified from the git history for this release.

## Migration

No migration is required from `v0.2.4`.

## Known Limitations

- No new known limitations are identified from the git history for this release.
- Published-image smoke testing should be performed after tag publication.

## Conformance

No new conformance behavior is identified from the git history for this release. No full release-gate transcript was recorded in this reconstructed note.
