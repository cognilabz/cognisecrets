# v0.2.2 Release Notes

## Status

`v0.2.2` is a manifest-backed CogniSecrets release reconstructed from git history.

Release date: 2026-08-04

Tag commit:

```text
a444dbe
```

Release image:

```text
ghcr.io/cognilabz/cognisecrets:v0.2.2
```

## Changes Since v0.1.0

- Added rendered install manifests under `manifests/`.
- Updated release automation to render manifests with the resolved image tag.
- Improved semantic version handling in the release workflow.
- Updated CI and release documentation for the Kustomize-based manifest layout.
- Expanded local release validation, E2E coverage tracking, and release-note guidance.

## API And Compatibility

No user-facing `SecretReference` API schema change is identified from the git history for this release.

The served API remains:

```text
cognilabz.com/v1
```

## Migration

No manifest schema migration is required from `v0.1.0`.

Users may install the release manifest through:

```sh
kubectl apply -k manifests
```

## Known Limitations

- `WriteFailed` conformance remains reference-implementation unit-test coverage only because operational Kubernetes write and delete failures are not portable to force in a black-box E2E suite.
- Published-image smoke testing should be performed after tag publication.

## Conformance

History in this range includes local release-test and E2E coverage updates. No full release-gate transcript was recorded in this reconstructed note.
